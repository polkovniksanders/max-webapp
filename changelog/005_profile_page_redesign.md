# 005 — Редизайн ProfilePage по макету

**Дата:** текущая сессия
**Статус:** не закоммичено

## Что сделано

Полностью переписана ProfilePage: убрана дебаг-информация (bridge data), реализован production-дизайн по макету Figma.

### Изменённые файлы
- `src/pages/profile/ui/ProfilePage.tsx` — полная перезапись:
  - Аватар (фото из `photo_url` или буква-заглушка с фоном `--color-primary`)
  - Имя пользователя из MAX Bridge
  - Секция "Заказы" с карточкой + empty state "Заказов пока нет"
  - Кнопка "История заказов >" с chevron → навигация на `/orders`
  - Sticky кнопка "Задать вопрос" над navbar
- `src/pages/profile/ui/ProfilePage.module.css` — полная перезапись:
  - CSS переменные темы: `--color-primary`, `--bg-layout`, `--bg-secondary`, `--color-text`, `--color-text-secondary`, `--border-color-secondary`
  - Серый фон `--bg-secondary` между секциями
  - Sticky bottom bar над navbar (bottom: 56px)
- `src/shared/config/routes.ts` — добавлен маршрут `ORDERS: '/orders'`

### Что убрано
- Дебаг-секции: "Пользователь", "Сессия", "Техническая информация"
- Raw initData блок с кнопкой копирования
- Кнопка "Закрыть приложение"

### Что готово к расширению
- Секция "История заказов" — роут добавлен, страница не реализована
- Секция "Вы смотрели" — появится при наличии API истории просмотров

## Статистика

| Метрика | Значение |
|---|---|
| Файлов изменено | 2 |
| Файлов затронуто (routes) | 1 |
| Строк добавлено | ~130 |
| Строк удалено | ~190 |
| Итого строк кода | ~320 |
| Токены сессии (approx.) | ~5 000 |
